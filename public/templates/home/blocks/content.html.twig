{% set payload = {
    'current_project': current_project.toArray,
    'force_embed_consent': force_embed_consent,
    'content': block.config.content,
} %}

{% sandbox %}
    {% include '@theme/home-custom-content.html.twig' with payload %}
{% endsandbox %}
