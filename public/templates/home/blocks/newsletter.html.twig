{% set payload = {
    'current_project': current_project.toArray,
    'enable_embed_consent': enable_embed_consent,
    'newsletter_title': current_project.terminology.newsletter,
    'newsletter_form': newsletterForm,
    'main_image': current_project.mainImage,
    'legalities': 'base.form_legalities'|trans,
} %}

{% sandbox %}
    {% include '@theme/home-newsletter.html.twig' with payload %}
{% endsandbox %}
